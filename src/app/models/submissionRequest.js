export class SubmissionRequest {
    constructor(client, tm_start, brick_size, tapes) {
        this.client = client;
        this.tm_start = tm_start;
        this.states = states;
        this.brick_size = brick_size;
        this.tapes = tapes;
    }
}

